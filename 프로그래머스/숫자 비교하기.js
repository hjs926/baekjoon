const solution = (num1, num2) => (num1 === num2 ? 1 : -1);

// 비교 연산자
// ==, != << 사용x

function solution(num1, num2) {
  var answer = num1 === num2 ? 1 : -1;
  return answer;
}

function solution(num1, num2) {
  if (num1 === num2) {
    return 1;
  } else {
    return -1;
  }
}
