function solution(num1, num2) {
  var answer = num1 * num2;
  return answer;
}

function solution(num1, num2) {
  var answer = num1 - num2;
  return answer;
}

const solution = (num1, num2) => num1 - num2;

function solution(num1, num2) {
  var answer = -1;

  return num1 + num2;
}
